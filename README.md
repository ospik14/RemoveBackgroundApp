# RemoveBG Service 🖼️

Сервіс для автоматичного видалення фону з зображень. Розроблений як Pet-проєкт для вивчення мікросервісної архітектури.

## 🛠️ Технічний стек (Tech Stack)

Проєкт побудований на сучасних асинхронних технологіях Python.

### Backend & API
* **Python 3.11** — основна мова розробки.
* **FastAPI** — високопродуктивний веб-фреймворк для побудови API. Використовується для обробки запитів та валідації даних.
* **Uvicorn** — блискавичний ASGI-сервер.
* **Pydantic** — для валідації вхідних даних та серіалізації відповідей.

### Робота з зображеннями (Image Processing)
* **Rembg** — бібліотека на базі Machine Learning (U2-Net) для видалення фону.
* **Pillow (PIL)** — для маніпуляцій із зображеннями (зміна розміру, конвертація форматів).
* **Python-multipart** — для обробки завантаження файлів (UploadFile).

### База даних (Database)
* **PostgreSQL 15** — реляційна база даних для зберігання історії обробок та метаданих файлів.
* **SQLAlchemy (Async)** — ORM для асинхронної роботи з базою даних.
* **Alembic** — інструмент для міграцій бази даних (контроль версій БД).

### Інфраструктура (DevOps)
* **Docker & Docker Compose** — повна контейнеризація всіх сервісів (app, db).

## 🚀 Функціонал

* Завантаження зображення через API.
* Обробка зображення (видалення фону).
* Збереження метаданих у базу даних PostgreSQL.
* Повернення обробленого результату.

## 📦 Як запустити (Installation)

Проєкт повністю докеризований. Для запуску потрібно мати встановлений **Docker**.

1. **Клонувати репозиторій:**
   ```bash
   git clone [https://github.com/TviyNick/removebg-service.git](https://github.com/TviyNick/removebg-service.git)

2. **Запустити через Docker Compose:**
    ```bash
    docker compose up --build

3. **Доступ до документації API (Swagger):**
    Відкрийте в браузері: http://localhost:8000/docs